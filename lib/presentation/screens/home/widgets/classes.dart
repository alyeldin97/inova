import 'package:flutter/material.dart';

class SeriesClasses extends StatelessWidget {
  const SeriesClasses({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
